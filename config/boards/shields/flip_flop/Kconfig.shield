# No whitespace after the comma or in your part name!
config SHIELD_FLIP_FLOP_LEFT
    def_bool $(shields_list_contains,flip_flop_left)

# No whitespace after the comma or in your part name!
config SHIELD_FLIP_FLOP_RIGHT
    def_bool $(shields_list_contains,flip_flop_right)