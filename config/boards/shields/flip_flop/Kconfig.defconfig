if SHIELD_FLIP_FLOP_LEFT

config ZMK_KEYBOARD_NAME
    default "FlipFlop"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_FLIP_FLOP_LEFT || SHIELD_FLIP_FLOP_RIGHT

config ZMK_SPLIT
    default y

endif